{
  "name": "logical-lunge-ui",
  "private": true,
  "description": "Logical Lunge widgets: bundled at build time (node build.mjs <out>), nothing is loaded from the network at runtime.",
  "scripts": {
    "build": "node build.mjs"
  },
  "dependencies": {
    "@tauri-apps/api": "2.10.1",
    "luxon": "3.4.4",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "zod": "3.24.2"
  },
  "devDependencies": {
    "esbuild": "0.28.2"
  }
}
